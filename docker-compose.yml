version: '3.8'

services:
  app:
    build: .
    container_name: gdpr-user-registry
    volumes:
      - database_data:/app/data
    stdin_open: true
    tty: true
    command: ["python", "test_gdpr.py"]

volumes:
  database_data:


